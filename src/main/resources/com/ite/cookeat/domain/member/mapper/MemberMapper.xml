<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ite.cookeat.domain.member.mapper.MemberMapper">
    <select id="selectUserDetails" parameterType="String" resultType="GetUserDetailsRes">
        <![CDATA[
            SELECT nickname, TO_CHAR(profile_img) as profileImage, one_liner, subscription_count as subscriptionCount, ssgcook_count as sskcookCount
            FROM member
            WHERE username = #{username} AND status = 'ACTIVE'
        ]]>
    </select>
</mapper>